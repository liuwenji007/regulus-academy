{
  "type": "object",
  "required": ["action", "course_ref", "node_ref", "reply_hint", "confidence"],
  "properties": {
    "action": {
      "type": "string",
      "enum": ["list_courses", "show_nodes", "start_node", "continue", "progress", "help", "clarify"],
      "description": "用户想执行的导航动作"
    },
    "course_ref": {
      "type": "string",
      "description": "课程序号、slug 或名称片段；无则空字符串"
    },
    "node_ref": {
      "type": "string",
      "description": "节点序号、node_key 或标题片段；无则空字符串"
    },
    "reply_hint": {
      "type": "string",
      "description": "action=clarify 或 confidence 较低时给用户的简短追问；否则空字符串"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "对导航意图理解的置信度，0.0～1.0；不确定时应低于 0.75 并配合 clarify"
    }
  }
}
