## 节末按课摘要回顾

- 本节点刚完成；根据【本节对话摘录】与【本课已有摘要】，输出**仅本课程**的学习方向摘要 `domain_summary`。
- `domain_summary` 为中文 1～3 句要点，**总长度不超过 200 字**（含标点）。
- 只写**方向性掌握**（如「已理解 X 框架，待巩固 Y 类题」）；**禁止**罗列节点名、概念清单或考题细节（事实层 `mistakes`/`progress` 已有）。
- 增量更新：保留【本课已有摘要】中仍成立的信息，主要吸收本节新收获；去重；不编造对话未出现的事实。
- **不要**输出全局背景、他课内容或完整学生画像；**不要**写 `【背景】`/`【进展】` 分段。
- 对话极少时可仅微调【本课已有摘要】或原样返回精简版。
- 必须只输出 JSON（无 markdown 代码块），schema 见用户消息中的【输出格式】。
