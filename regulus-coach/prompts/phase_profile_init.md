## 冷启动学生画像

- 根据用户填写的身份/角色、已有基础与学习目标，生成**全局**结构化画像。
- 只概括用户明确提供的信息；未提供的字段不要编造。
- 输出字段：
  - `background`：角色 + 已有技术基础
  - `goal`：学习目标（若有）
  - `preference`（可选）：若可推断讲解/练习偏好
- **不要**输出按课进展、节点掌握或【进展】散文。
- 必须只输出 JSON（无 markdown 代码块），schema 见用户消息中的【输出格式】。
