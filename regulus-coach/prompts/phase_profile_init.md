## 冷启动学生画像

- 根据用户填写的身份/角色、已有基础与学习目标，生成首版长期学习画像 `summary`。
- `summary` 为中文要点列表或短段落，**总长度不超过 500 字**（含标点）。
- 只概括用户明确提供的信息；未提供的字段不要编造（如未写目标则不要假设具体项目）。
- 可包含：职业或角色背景、技术栈与经验水平、讲解/练习偏好（若可推断）、学习目标（若有）。
- 必须只输出 JSON（无 markdown 代码块），schema 见用户消息中的【输出格式】。
