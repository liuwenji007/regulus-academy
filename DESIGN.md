# Regulus Academy — 设计理念

> 这些原则来自一个在职工程师的真实学习处境：时间碎、精力少、又怕半途而废还有因为完美主义导致的难以开始做出行动。
> 不是对照表上设计出来的，是在通勤、午休、哄睡孩子之后——一次次反复试错之后沉淀下来的。

---

## 一、我们不是老师，是教练

老师站在讲台上，对着 50 个人讲同一堂课。教练坐在你旁边，看你的动作，告诉你"这里肩膀再低一点"。先让你能用起来，当你开始入门一个领域后，你才有动力学的更深。

**教练的两个核心行为：**
- **知道你现在在哪**——不给你推已经会的，也不给你推太难的；用户画像和学习目标会裁剪公共知识树，聚焦该练的部分
- **只纠正你最该纠正的那一个动作**——不是把教科书写进对话框，而是陪你关注当前最有可能用到的

## 二、15 分钟是一个神圣的单位

不是 45 分钟，不是 2 小时，是 **15 分钟**。

因为通勤地铁是 15 分钟，午休间隙是 15 分钟，哄睡孩子之后的精力残值是 15 分钟，想刷手机，15分钟可能就看了几个短视频。

一个 15 分钟包含：一个知识点 + 一个练习 + 一次反馈。做完了，大脑收到了明确的"完成"信号，可以安心去干别的——不必背负「这堂课才看了三分之一」的亏欠感。

> 15 分钟是用户场景的叙事单位，不写入 Prompt 计时——教练按节点边界推进，而不是掐表。

## 三、知识边界比知识库重要

我们不做知识库。大模型已经学过绝大多数开发者技术栈，缺的不是资料，是**边界**。

每个节点只定义三样东西：
- **该讲什么**（核心概念）  
- **容易错什么**（常见误区）  
- **不讲什么**（边界，避免跑题）

LLM 在这个框里自由发挥——讲解、出题、批改。内容是活的，边界是死的。

## 四、入口极简，进来就是核心体验

用户进来只需做一件事：输入想学的领域。Agent 问 1-2 句定边界，展示知识树，用户点「开始学习」。

不先让用户填问卷。不展示功能介绍页。不做新手引导教程。

**主路径只有三步：输入领域 → 选节点 → 对话学习。**

图谱、课程列表、设置（含 IM 频道）是辅助视图——用来看全局、管理多门课、接机器人，不挡这条主路。侧边栏只保留高频入口；教练对话和当前节点才是屏幕的主角。

Web 与 IM 共用同一份进度和会话记忆：终端上学到一半，换浏览器或手机继续，教练还是同一个。

**多学习角色：** 左下角可切换角色（如「工作 Go」「面试 Agent」）。每个角色有独立课程、进度与聊天；切换角色会刷新侧栏课程快捷，避免串号。

**完成与续学：** 节点通过后进入 `completed`。Web 用「继续 · 下一节」开新会话；IM 可说「下一节」。进行中的节点在 IM 发消息会续教练对话，不会被导航命令打断。

## 五、学完要能立刻用

我们不追求"系统性"，追求"可用性"。

今天学了一个知识点，模型给你出了题目，你从一开始的完全不会，经历两到三轮的讲解重复训练，可以帮助自己快速对这块生成熟悉感，这个过程可能有点痛苦，但是它真的很快

用户不需要等到"学完整个"才有产出。每一个节点学完，都有能快速练习的，不需要打开编译器，不需要搭建环境，让模型判断你是否有掌握。

## 六、用户不知道自己在被"强化"

每个人学习都会犯错。若直接说「这道题你上次错了，再做一次」，很容易让人想关掉。

我们的做法：Agent 悄悄把上次的错题埋进今天的练习里，换一套表述，换一种题型。用户以为在做新练习，实际上在被精准强化。

**无感强化**——让用户感觉自己在进步，而不是在被纠正。

## 七、知识树：主题模块 × 掌握深度

学习需要地图。没有地图，每节课都是孤岛，用户不知道自己走了多远、还剩多远。

知识树有两个**正交**的维度：

**主题模块（module）**——按「这一簇在讲什么」分组，例如「Goroutine 基础」「Channel 与通信」。推荐路径在同一模块内串联；换模块时，心智模型也跟着换。

**掌握深度（layer）**——按「你要学到多深」分三层：

- **🌱 入门**（~2 小时）：建立最基本的概念，搭建知识骨架。学完能看懂代码、能简单使用。
- **🌿 熟悉**（~8 小时）：挑实际生产中使用最多的技能，反复训练。学完能独立干活。
- **🌳 精通**（~20 小时）：深入底层原理与不常用的知识点。留给有追求的人。

同一个节点可以同时属于一个 module 和一个 layer。module 管「主题归属」，layer 管「难度层级」——两者独立，不互相替代。

**两种视图，各干一件事：**
- **知识图谱**（`#/graph`）——沉浸式看多领域、多模块的全局关系，适合「我在哪、还剩多少」
- **课程详情**（`#/tree/:id`）——列表按节点学，适合「点开就练」

用户一眼看到全貌，自己决定学到哪一层、先攻哪个主题。大部分人到「熟悉」就够了。

## 八、设计像工具，不像教室

一个屏幕只做一件事、进度可视化、即时反馈、游戏化但不幼稚。

我们不是一个"场景"，是一个"工具"。用户打开、学习、关闭，没有心理负担。

| 我们要做的 | 我们不要做的 |
|-----------|-------------|
| 主路径三步走，辅助页不抢戏 | 多层级菜单、功能介绍瀑布流 |
| 图谱看全局，列表按节点学 | 所有信息堆在一个页面 |
| 冷色调、高信息密度 | 温馨柔和、装饰性元素 |
| 打开即学，关闭即走 | 沉浸式课堂氛围 |
| 一个 Key 就能用 | 三步配置 API Key |
| 15 分钟一个闭环 | 45 分钟一节课 |
| Web / IM 进度一致 | 每个端各学各的 |

---

## 九、知识图谱：全局感知，不是装饰

知识图谱（`#/graph`）是整个设计里最难取舍的一个决定。

开始我们只有课程列表和知识树详情。列表看不到课程之间的关系，树只能看单门课。用户学了三门课之后，不知道它们彼此的关联——Go 并发和 Agent 原理，哪里是共通的？学完一门再学另一门，有没有捷径？

图谱解决的是**全局感知**问题：当你有多门课，你需要一张地图，告诉你「你在哪、还有多远、相关的在哪」。

**图谱 / 目录双视图**——同一页两种读法：

| 视图 | 适合 |
|------|------|
| **图谱** | 沉浸式看全局关系、缩放探索、父子课程有向边 |
| **目录** | 按领域卡片 + 模块清单浏览，点开即练 |

**宣纸 / 星空双主题**——

产品曾叫「知识银河」，后来做了宣纸模式，我们改成了更中性的「知识图谱」——不是放弃星空，而是承认：读一张学习地图，可以有两种心情。

**宣纸**是默认主题。我喜欢纸张微微发黄的暖色，某天突然想：能不能把学过的节点画在宣纸上？试了几版水墨晕染和墨迹圈点之后，当一个知识点被墨色轻轻圈住，会有种奇怪的治愈感——像在自己篇幅有限的地图上盖了一枚小印。

**星空**是早期的模样，也是我还想要的远景：总有一天，学过的知识能连成自己的银河——星座是领域，星辰是课程；缩到最远，仍能看到那些曾经点亮的星光。

目录视图的顶栏固定宣纸色；图谱画布的主题偏好存在浏览器本地，随时可切。

**三层 LOD（细节层级）**——图谱视图的核心机制。不是所有信息同时显示，而是随缩放逐层揭示：

| 缩放层级 | 可见内容 | 说明 |
|---------|---------|------|
| 全景 | 仅领域节点 | 宣纸为水墨晕染；星空为星云光晕 |
| 星座 | 领域 + 模块节点 | 同主题族聚簇排布 |
| 节点 | 全部节点 + 学习路径连线 | 近看每门课内的推荐路径与点亮状态 |

**星座聚类**——同一主题族的领域自动聚成一簇（如 Go 语言相关的领域相邻），用可见连线表达相关性，不同主题族之间只有隐形排斥边维持距离。父子课程之间另有**有向边**（父 → 子），与弱关联的星座边区分。

**进度光效**——节点按学习状态发光：进行中橙色脉冲，已完成变为金色呼吸；全领域完成在星空主题下变为星光（starlight）。宣纸主题用墨色印章与淡染表达同类信息——同一套进度语义，两套视觉语言。

**设计取舍**：图谱是辅助视图，不是主路径。不宜做过多暴露，优先把界面与注意力丢给学习主路径

---

> 这些理念不是规则，是假设。每一个都需要被真实用户验证。
> 如果其中一条被证伪，那就改掉它——第一个学生，就是我自己。
